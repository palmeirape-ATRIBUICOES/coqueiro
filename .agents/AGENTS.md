# Custom Rules for Coqueiro Project

- **Direct GitHub Deployments:** Whenever any code change is implemented, stage, commit, and push it immediately to the `main` branch on GitHub so that it triggers the automated GitHub Actions deployment. The user does not test changes locally; all verification is done directly online on the hosted staging URL.
